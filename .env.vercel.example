# ==================================================
# VERCEL PRODUCTION ENVIRONMENT VARIABLES
# ==================================================
# Copy nilai ini ke Vercel Dashboard > Settings > Environment Variables
# ==================================================

# App Configuration
APP_NAME=ZyaShop
APP_ENV=production
APP_DEBUG=false
APP_URL=https://zyashop.vercel.app
APP_KEY=base64:TGaVaXwhWgjn9akhE4SCSIjGG/cQqvICUQ+PgLA3Bs0=
APP_TIMEZONE=Asia/Jakarta
APP_LOCALE=id

# Cache Configuration (Use /tmp for serverless)
APP_CONFIG_CACHE=/tmp/config.php
APP_EVENTS_CACHE=/tmp/events.php
APP_PACKAGES_CACHE=/tmp/packages.php
APP_ROUTES_CACHE=/tmp/routes.php
APP_SERVICES_CACHE=/tmp/services.php
VIEW_COMPILED_PATH=/tmp/views

# Session Configuration (CRITICAL FOR VERCEL!)
# MUST use 'cookie' driver for serverless - file/database won't persist
SESSION_DRIVER=cookie
SESSION_LIFETIME=10080
SESSION_ENCRYPT=false
SESSION_PATH=/
# IMPORTANT: Leave empty for Vercel subdomain, OR set to your custom domain
SESSION_DOMAIN=null
# MUST be true for HTTPS (Vercel uses HTTPS)
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax
SESSION_COOKIE=zyashop_session

# Cache & Queue (Serverless compatible)
CACHE_DRIVER=array
QUEUE_CONNECTION=sync
LOG_CHANNEL=stderr
LOG_LEVEL=error

# Database Configuration (Filess.io MySQL)
DB_CONNECTION=mysql
DB_HOST=5h8j1o.h.filess.io
DB_PORT=61002
DB_DATABASE=ZyaShop_cattaskfog
DB_USERNAME=ZyaShop_cattaskfog
DB_PASSWORD=a81b358b3b11eebbe3adce8c61e5454d546ac773
MYSQL_ATTR_SSL_CA=

# ==================================================
# NOTES:
# - SESSION_DRIVER=cookie adalah WAJIB untuk Vercel (serverless)
# - SESSION_SECURE_COOKIE=true karena Vercel pakai HTTPS
# - SESSION_DOMAIN=null agar cookie disimpan untuk exact domain
# - Jika pakai custom domain: SESSION_DOMAIN=.yourdomain.com
# ==================================================
